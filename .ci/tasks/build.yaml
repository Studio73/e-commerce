---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: concourse/oci-build-task
inputs:
  - name: repo
  - name: base-image
outputs:
  - name: image
run:
  path: build
caches:
  - path: cache
