name: pr
on:
  workflow_dispatch:
  pull_request:
    branches:
      - {{ odoo_version }}.0
    types: [ready_for_review, opened, synchronize, reopened]
jobs:
  ci:
    uses: ./.github/workflows/ci.yaml
    secrets: inherit
